---
description: Show where prod-guard friction accumulates, from your local Claude Code session transcripts.
argument-hint: "[--since 24h] [--repo name] [--json] [--top N]"
allowed-tools: Bash
---

The prod-guard friction report below is generated by re-reading the hook
decisions Claude Code already recorded in your local session transcripts (it adds
no telemetry — see PRIVACY.md). Show it to me verbatim, then add at most one
sentence naming the biggest source of friction. Do not edit any files.

If the report lists unclassified targets, those are pattern gaps: vet each one
and, if it is non-prod, add a `nonprod` pattern to `.claude/prod-guard.json` (see
the prod-guard README Configuration section). Never reclassify a real production
target as non-prod to silence a prompt.

!`python3 "${CLAUDE_PLUGIN_ROOT}/scripts/friction-report.py" $ARGUMENTS`
